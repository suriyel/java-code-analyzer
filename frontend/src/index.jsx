import React from 'react';
import ReactDOM from 'react-dom/client';
import CodeAnalyzerApp from './App';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <CodeAnalyzerApp />
    </React.StrictMode>
);